#include <iostream>

using namespace std;

int main()
{
    string a, b;
    cin >> a >> b;
    int la = a.size(), lb = b.size();
    
    for (int i = 0; i < lb - la; i++)
    {
        cout << b[i];
    }

    for (int i = )
}