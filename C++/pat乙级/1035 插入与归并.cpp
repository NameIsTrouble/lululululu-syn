#include <iostream>
#include <algorithm>

using namespace std;

string temp, a, b;

int main()
{
    int N;
    cin >> N;
    
    for (int i = 0; i < N; ++i)
        cin >> a[i];

    a[N] = '\0';
    b = a;

    
}